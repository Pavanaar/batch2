import { expect } from "chai"

describe("",()=>{
    it("",async()=>{
        await expect(10).to.be.a(Number)
        let obj={
            name:"pavana",
            lastnm:"r"
        }
        expect(obj).to.be.equal.to('object')
    })
})